apply plugin: 'java'

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile project(':libjava')
    compile rootProject.ext.greendaogenerate
}

tasks.withType(JavaCompile) {
    options.encoding = "UTF-8"
}

